<Window x:Class="SmartERP.UI.Views.UserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add/Edit User"
        Height="600"
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="ComboBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style TargetType="CheckBox">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style x:Key="SaveButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#388E3C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CancelButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#757575"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#616161"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="#007ACC" 
                Padding="20,15">
            <TextBlock x:Name="DialogTitle"
                      Text="Add New User"
                      FontSize="20"
                      FontWeight="Bold"
                      Foreground="White"/>
        </Border>

        <!-- Form Content -->
        <ScrollViewer Grid.Row="1" 
                     VerticalScrollBarVisibility="Auto"
                     Padding="30,20">
            <StackPanel>
                <!-- Username -->
                <TextBlock Text="Username *" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="UsernameTextBox" Margin="0,0,0,15"/>

                <!-- Full Name -->
                <TextBlock Text="Full Name *" Style="{StaticResource LabelStyle}"/>
                <TextBox x:Name="FullNameTextBox" Margin="0,0,0,15"/>

                <!-- Email and Phone -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Email *" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="EmailTextBox"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Phone Number" Style="{StaticResource LabelStyle}"/>
                        <TextBox x:Name="PhoneNumberTextBox"/>
                    </StackPanel>
                </Grid>

                <!-- Role -->
                <TextBlock Text="Role *" Style="{StaticResource LabelStyle}"/>
                <ComboBox x:Name="RoleComboBox" Margin="0,0,0,15">
                    <ComboBoxItem Content="Admin" Tag="Admin"/>
                    <ComboBoxItem Content="User" Tag="User" IsSelected="True"/>
                </ComboBox>

                <!-- Password Section (Only for New Users) -->
                <StackPanel x:Name="PasswordPanel" Margin="0,0,0,15">
                    <Border Background="#FFF3E0" 
                           Padding="15" 
                           CornerRadius="5"
                           Margin="0,0,0,10">
                        <TextBlock Text="Set initial password for new user. Users can change their password later."
                                  TextWrapping="Wrap"
                                  FontSize="12"
                                  Foreground="#F57C00"/>
                    </Border>

                    <TextBlock Text="Password *" Style="{StaticResource LabelStyle}"/>
                    <PasswordBox x:Name="PasswordBox" Margin="0,0,0,10"/>

                    <TextBlock Text="Confirm Password *" Style="{StaticResource LabelStyle}"/>
                    <PasswordBox x:Name="ConfirmPasswordBox" Margin="0,0,0,10"/>
                </StackPanel>

                <!-- Active Status -->
                <CheckBox x:Name="IsActiveCheckBox" 
                         Content="Active User" 
                         IsChecked="True"
                         Margin="0,0,0,15"/>

                <!-- Info Panel for Edit Mode -->
                <Border x:Name="InfoPanel"
                       Background="#E3F2FD" 
                       Padding="15" 
                       CornerRadius="5"
                       Margin="0,0,0,15"
                       Visibility="Collapsed">
                    <StackPanel>
                        <TextBlock Text="Note: Password cannot be changed from this dialog. Use 'Change Password' button for password updates."
                                  TextWrapping="Wrap"
                                  FontSize="12"
                                  Foreground="#1976D2"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Action Buttons -->
        <Border Grid.Row="2" 
                Background="White"
                BorderThickness="0,1,0,0"
                BorderBrush="#E0E0E0"
                Padding="20,15">
            <StackPanel Orientation="Horizontal" 
                       HorizontalAlignment="Right">
                <Button Content="Save" 
                       Style="{StaticResource SaveButtonStyle}"
                       Click="SaveButton_Click"/>
                <Button Content="Cancel" 
                       Style="{StaticResource CancelButtonStyle}"
                       Click="CancelButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
