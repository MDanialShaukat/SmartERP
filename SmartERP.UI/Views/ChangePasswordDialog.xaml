<Window x:Class="SmartERP.UI.Views.ChangePasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change Password"
        Height="560"
        Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F5F5F5">
    
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="LabelStyle">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>

        <Style TargetType="PasswordBox">
            <Setter Property="Height" Value="35"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="BorderBrush" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="SaveButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4CAF50"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#388E3C"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="CancelButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#757575"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#616161"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" 
                Background="#FF9800" 
                Padding="20,15">
            <StackPanel>
                <TextBlock Text="Change Password"
                          FontSize="20"
                          FontWeight="Bold"
                          Foreground="White"/>
                <TextBlock x:Name="UserInfoText"
                          Text="Username"
                          FontSize="14"
                          Foreground="White"
                          Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Form Content -->
        <StackPanel Grid.Row="1" 
                   Margin="30,20">
            
            <!-- Info Panel -->
            <Border Background="#E3F2FD" 
                   Padding="15" 
                   CornerRadius="5"
                   Margin="0,0,0,20">
                <TextBlock Text="As an administrator, you can set a new password for this user. The user will be able to login with the new password immediately."
                          TextWrapping="Wrap"
                          FontSize="12"
                          Foreground="#1976D2"/>
            </Border>

            <!-- New Password -->
            <TextBlock Text="New Password *" Style="{StaticResource LabelStyle}"/>
            <PasswordBox x:Name="NewPasswordBox" Margin="0,0,0,15"/>

            <!-- Confirm Password -->
            <TextBlock Text="Confirm New Password *" Style="{StaticResource LabelStyle}"/>
            <PasswordBox x:Name="ConfirmPasswordBox" Margin="0,0,0,15"/>

            <!-- Password Requirements -->
            <Border Background="#FFF3E0" 
                   Padding="15" 
                   CornerRadius="5"
                   Margin="0,0,0,15">
                <StackPanel>
                    <TextBlock Text="Password Requirements:" 
                              FontSize="12"
                              FontWeight="SemiBold"
                              Foreground="#F57C00"
                              Margin="0,0,0,5"/>
                    <TextBlock Text="- Minimum 6 characters" 
                              FontSize="11"
                              Foreground="#F57C00"/>
                    <TextBlock Text="- Recommended: Mix of letters, numbers, and symbols" 
                              FontSize="11"
                              Foreground="#F57C00"/>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Action Buttons -->
        <Border Grid.Row="2" 
                Background="White"
                BorderThickness="0,1,0,0"
                BorderBrush="#E0E0E0"
                Padding="20,15">
            <StackPanel Orientation="Horizontal" 
                       HorizontalAlignment="Right">
                <Button Content="Change Password" 
                       Style="{StaticResource SaveButtonStyle}"
                       Click="ChangePasswordButton_Click"/>
                <Button Content="Cancel" 
                       Style="{StaticResource CancelButtonStyle}"
                       Click="CancelButton_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
